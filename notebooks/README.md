# Notebooks — Does the Emoji Matter?

The four-stage pipeline behind the study. Each stage reads what the previous one
wrote, so **run them in order**: `01` → `02` → `03` → `04`.

Every notebook has a companion `*_explanation.md` in this folder: a section-by-
section walk-through with the actual numbers, why each decision was made, and the
caveats that apply.

For project setup, data download and the Streamlit app, see the
[root README](../README.md).

---

## The four stages at a glance

| Notebook | Outputs | Figures |
|---|---|---|
| **[01_data_load](01_data_load.ipynb)**<br>[explanation](01_data_load_explanation.md) | `data/interim/train_raw.csv`<br>`data/interim/dev_raw.csv`<br>`data/interim/test_raw.csv` | *none* |
| **[02_eda](02_eda.ipynb)**<br>[explanation](02_eda_explanation.md) | `results/eda_text_quality.csv`<br>`results/eda_emoji_density.csv` | `label_distribution.png`<br>`label_cardinality.png`<br>`label_cooccurrence.png`<br>`text_length.png`<br>`emoji_label_skew.png` |
| **[03_model_train](03_model_train.ipynb)**<br>[explanation](03_model_train_explanation.md) | `data/processed/{train,dev,test}_cleaned.csv`<br>`models/lstm_no_emoji.keras`<br>`models/lstm_with_emoji.keras`<br>`models/bert_no_emoji.keras`<br>`models/bert_with_emoji.keras`<br>`results/thresholds.json`<br>`results/best_configs.json`<br>`results/hyperparameter_search.csv`<br>`results/pipeline_fixture.json` | *none* |
| **[04_evaluation](04_evaluation.ipynb)**<br>[explanation](04_evaluation_explanation.md) | `results/results_table.csv`<br>`results/emoji_impact.csv`<br>`results/per_emotion_impact.csv`<br>`results/per_emotion_auc.csv`<br>`results/mean_auc.csv`<br>`results/confusion_counts.csv`<br>`results/test_results_table.csv` *(opt-in, §4.9)* | `overall_performance.png`<br>`emoji_impact.png`<br>`per_emotion_impact.png`<br>`precision_recall.png`<br>`confusion_{lstm,bert}_{no,with}_emoji.png` *(4)*<br>`roc_{lstm,bert}_{no,with}_emoji.png` *(4)* |

Totals: **17 figures** and **13 result files** are generated by the notebooks.
Figures in `figures/` that are **not** produced here — `arch_*.png`,
`gantt_planned_vs_actual.png`, `risk_matrix.png`, `app_*.png` — belong to the
presentation and the report, not to the pipeline.

---

## What each stage does

| # | Notebook | In one line | Runtime |
|---|---|---|---|
| 1 | `01_data_load` | Load the corpus, standardise the schema, normalise labels, assert integrity | seconds, CPU |
| 2 | `02_eda` | Profile the raw corpus — imbalance, co-occurrence, text noise, emoji density | <1 min, CPU |
| 3 | `03_model_train` | Fork the pipeline into two tracks, train both architectures on each, save models + thresholds | ~75 min on a GPU |
| 4 | `04_evaluation` | Score all four conditions, compute ΔF1, write every table and figure the report cites | ~2 min, CPU |

Stage 3 is the only one the Streamlit UI strictly needs — it writes straight into
the folders the app already reads, so there is no copying step.

---

## How the data flows

```
data/raw/          three SemEval .txt files       (python download_data.py)
   │  01
data/interim/      {train,dev,test}_raw.csv
   │  02  ──▶  figures/ (5), results/eda_*.csv
   │  03
data/processed/    {train,dev,test}_cleaned.csv   ← both tracks, side by side
models/            4 × .keras                     ← read by the UI
results/           thresholds.json, best_configs.json, hyperparameter_search.csv
   │  04
results/           results_table, emoji_impact, per_emotion_impact,
                   per_emotion_auc, mean_auc, confusion_counts
figures/           12 more
   │
app/streamlit_app.py
```

---

## The experimental design in one paragraph

One preprocessing pipeline is built and forked at exactly one operation.
**Track A** deletes every emoji; **Track B** replaces each with its Unicode
description (`😂` → `face with tears of joy`). Everything upstream and downstream
is identical, so the only systematic difference between the two corpora is
whether emoji content survives as text. Each track then trains both architectures
under an identical protocol, giving four comparable conditions and a clean
difference measure, `ΔF1 = F1(with emoji) − F1(without emoji)`.

|  | Track A — no emoji | Track B — with emoji |
|---|---|---|
| **BiLSTM** | `lstm_no_emoji.keras` | `lstm_with_emoji.keras` |
| **Transformer** | `bert_no_emoji.keras` | `bert_with_emoji.keras` |

---

## Headline results

All figures are **development-split** (886 rows). The test split is prepared but
deliberately unconsumed — `04` §4.9 runs it behind an opt-in flag.

| Model | Track | Micro F1 | Macro F1 | Mean AUC |
|---|---|---|---|---|
| BiLSTM | Without Emoji | 53.63 | 47.86 | 0.786 |
| **BiLSTM** | **With Emoji** | **55.65** | **48.86** | **0.788** |
| Transformer | Without Emoji | 47.29 | 44.36 | 0.754 |
| Transformer | With Emoji | 51.00 | 47.30 | 0.777 |

**RQ1 — aggregate emoji effect:** +2.03 ΔMicro-F1 (BiLSTM), +3.71 (Transformer).
Both positive; both smaller than the 7.38-point spread across hyper-parameter
configurations, so **indicative rather than confirmed**.

**RQ2 — does it depend on architecture and emotion?** On emotion, decisively:
per-emotion ΔF1 spans 15.9 points (`anticipation` −4.13 → `sadness` +11.80),
more than four times the largest aggregate effect. The emotions that gain —
`sadness`, `fear`, `joy`, `love` — are exactly those `02` identified as
over-represented among emoji-bearing tweets.

**The conclusion is therefore conditional:** emoji handling is a label-dependent
preprocessing decision that should be validated on the target task, not applied
as a blanket default.

---

## Running them

```bash
pip install -r ../requirements.txt
```

```bash
python ../download_data.py
```

Then run `01` → `02` → `03` → `04` in order. Afterwards:

```bash
streamlit run ../app/streamlit_app.py
```

All four notebooks share the same header cell, which auto-detects Kaggle
(`/kaggle/working`) and otherwise resolves the project root by walking up one
level from `notebooks/`. That shared header is also what makes
`kaggle_run_all.ipynb` buildable by plain concatenation, so the merged notebook
cannot drift from these four.

---

## Caveats that apply across the pipeline

**The attention model is not pretrained BERT.** It is a single Transformer
encoder block trained from scratch, with no positional encoding. The checkpoints
are named `bert_*.keras` for continuity with the original `krish.ipynb`, and
every table and figure relabels it `Transformer`. Its absolute scores sit below
published state of the art for exactly this reason.

**Every condition was trained once, with one seed.** There is no estimate of
run-to-run variance, and configuration choice alone moves Micro-F1 by 7.38 points
— twice the largest emoji effect measured. Training each condition across three
seeds is the most valuable extension available.

**The development split did triple duty** — model selection, threshold selection
and reporting. Absolute values are optimistic by construction. The bias applies
equally to both tracks, so the comparison between them still holds.

**The threshold sweep is truncated and it binds.** `03` sweeps `0.10 … 0.55`, and
all four conditions selected exactly **0.55**, the top of the range — the
signature of an optimum at or beyond the boundary. Widening the sweep needs no
retraining, only a re-scoring pass, and is the cheapest improvement available.
(Note: the root README's claim that thresholds "land well below 0.5" does not
match the saved `results/thresholds.json`, which is `0.55` for all four.)

**Emoji density is uneven across splits.** 11.3% of training rows carry emoji but
roughly 25% of dev and test rows do. The dilution argument holds, but on the
split the metrics come from it is about 75% of rows, not 89%.

**NLTK downloads failed in the saved Kaggle run** (no internet), so lemmatisation
was largely inert in the shipped models. Both tracks were affected identically,
so the comparison is unaffected — but a local re-run *with* internet will
lemmatise properly and will not reproduce the checkpoints bit-for-bit.

**Preprocessing must not drift.** `app/preprocessing.py` is a deliberate copy of
the pipeline defined in `03`, because the models were fitted on that exact output.
`03` also writes `results/pipeline_fixture.json` — input/output pairs the serving
copy can be checked against.

**Keras version drift is real.** The shipped models were saved by Keras 3.13.2 and
fail to load on 3.11 with `Unrecognized keyword arguments passed to Embedding:
{'quantization_config': None}`. `requirements.txt` pins `keras>=3.13,<3.14`.
