# Pinned where it matters. The shipped models were trained on Kaggle with
# Keras 3.13.2; loading them on Keras < 3.12 fails with
#   Unrecognized keyword arguments passed to Embedding: {'quantization_config': None}
# so keras is pinned rather than left to TensorFlow's own floor.
tensorflow>=2.19,<2.21
keras>=3.13,<3.14
streamlit>=1.40
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
plotly>=5.20
nltk>=3.8
emoji>=2.10
demoji>=2.0
matplotlib>=3.7
jupyter>=1.0
notebook>=7.0
